FunctionDef(
    ~name,
    decorator_list = [
        Name(~foo),
        Name(~foo)
    ],
    body = [
        Expr(Name(~foo)),
        Expr(Attribute(Name(~foo))),
        Return(Call(Name(~name)))
    ]
)
